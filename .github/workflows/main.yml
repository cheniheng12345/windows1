name: Wendaoshi-2k2
on:
  workflow_dispatch:
jobs:
  build:
    name: Start Building...
    runs-on: windows-latest
    timeout-minutes: 9999
    steps:
      - name: Set up Python 3.11
        uses: actions/setup-python@v4
        with:
          python-version: '3.11'

      - name: Download RustDesk portable
        run: |
          $url = "https://github.com/rustdesk/rustdesk/releases/download/1.2.3/rustdesk-1.2.3-x86_64.exe"
          $out = "$env:TEMP\rustdesk.exe"
          if (Test-Path $out) { Remove-Item $out -Force }
          $retry = 0
          do {
            try {
              Write-Host "Downloading from $url (attempt $($retry+1))..."
              Invoke-WebRequest -Uri $url -OutFile $out -UseBasicParsing
              $size = (Get-Item $out).Length
              if ($size -gt 5MB) {
                Write-Host "Download successful. Size: $([math]::Round($size/1MB, 2)) MB"
                break
              } else {
                Write-Host "File too small ($size bytes), retrying..."
                Remove-Item $out -Force
              }
            } catch {
              Write-Host "Download failed: $_"
            }
            $retry++
            Start-Sleep -Seconds 3
          } while ($retry -lt 3)
          if (-not (Test-Path $out) -or (Get-Item $out).Length -lt 5MB) {
            throw "Failed to download valid rustdesk.exe"
          }

      - name: Start RustDesk portable
        run: |
          Start-Process -FilePath "$env:TEMP\rustdesk.exe" -ArgumentList "--portable"
          Write-Host "RustDesk started. Waiting for window to appear..."
          Start-Sleep -Seconds 10

      - name: Click Install button
        run: |
          # 逐行写入 Python 脚本
          echo import pyautogui > "%TEMP%\click.py"
          echo import time >> "%TEMP%\click.py"
          echo time.sleep(5) >> "%TEMP%\click.py"
          echo screen_width, screen_height = pyautogui.size() >> "%TEMP%\click.py"
          echo click_x = screen_width // 2 >> "%TEMP%\click.py"
          echo click_y = int(screen_height * 0.7) >> "%TEMP%\click.py"
          echo print(f'Clicking at ({click_x}, {click_y})') >> "%TEMP%\click.py"
          echo pyautogui.click(click_x, click_y) >> "%TEMP%\click.py"
          echo print('Clicked Install. Waiting 30 seconds...') >> "%TEMP%\click.py"
          echo time.sleep(30) >> "%TEMP%\click.py"
          pip install pyautogui pillow
          python "%TEMP%\click.py"

      - name: Wait for password
        run: |
          Write-Host "Waiting additional 60 seconds for password generation..."
          Start-Sleep -Seconds 60

      - name: Take screenshot
        run: |
          pip install pyautogui pillow
          python -c "import pyautogui, time; time.sleep(3); pyautogui.screenshot('rustdesk_id.png')"

      - name: Upload screenshot as artifact
        uses: actions/upload-artifact@v4
        with:
          name: rustdesk-screenshot
          path: rustdesk_id.png

      - name: Keep runner alive forever
        run: |
          Write-Host "========================================"
          Write-Host "RustDesk is running (portable mode, Install clicked)."
          Write-Host "Workflow will run continuously until manually cancelled."
          Write-Host "Download the screenshot from Artifacts to see ID and password."
          Write-Host "========================================"
          while ($true) { Start-Sleep -Seconds 60 }
